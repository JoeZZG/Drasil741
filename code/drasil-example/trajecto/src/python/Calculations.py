## \file Calculations.py
# \author Zhuo Zhang
# \brief Provides functions for calculating the outputs
# \note Generated by Drasil v0.1-alpha

import scipy.integrate

import DetectorHit

## \brief Calculates dependent variables (m)
# \param κ charge-to-mass ratio (C/kg)
# \param E_x0 x-electric field in region 0 (N/C)
# \param E_x1 x-electric field in region 1 (N/C)
# \param E_x2 x-electric field in region 2 (N/C)
# \param E_x3 x-electric field in region 3 (N/C)
# \param E_x4 x-electric field in region 4 (N/C)
# \param E_x5 x-electric field in region 5 (N/C)
# \param x_grid grid origin x-coordinate (m)
# \param N_col number of grid columns
# \param w region width (m)
# \param y_grid grid origin y-coordinate (m)
# \param h region height (m)
# \param B_0 magnetic flux density in region 0 (T)
# \param B_1 magnetic flux density in region 1 (T)
# \param B_2 magnetic flux density in region 2 (T)
# \param B_3 magnetic flux density in region 3 (T)
# \param B_4 magnetic flux density in region 4 (T)
# \param B_5 magnetic flux density in region 5 (T)
# \param E_y0 y-electric field in region 0 (N/C)
# \param E_y1 y-electric field in region 1 (N/C)
# \param E_y2 y-electric field in region 2 (N/C)
# \param E_y3 y-electric field in region 3 (N/C)
# \param E_y4 y-electric field in region 4 (N/C)
# \param E_y5 y-electric field in region 5 (N/C)
# \param x_0 initial x-position (m)
# \param y_0 initial y-position (m)
# \param v_x0 initial x-velocity (m/s)
# \param v_y0 initial y-velocity (m/s)
# \param t_final final simulation time (s)
# \return dependent variables (m)
def func_s(κ, E_x0, E_x1, E_x2, E_x3, E_x4, E_x5, x_grid, N_col, w, y_grid, h, B_0, B_1, B_2, B_3, B_4, B_5, E_y0, E_y1, E_y2, E_y3, E_y4, E_y5, x_0, y_0, v_x0, v_y0, t_final):
    def f(t, s):
        return [s[2], s[3], κ * ((E_x0 if s[0] >= x_grid + 0 % N_col * w and (s[0] < x_grid + (0 % N_col + 1) * w and (s[1] >= y_grid + 0 // N_col * h and s[1] < y_grid + (0 // N_col + 1) * h)) else E_x1 if s[0] >= x_grid + 1 % N_col * w and (s[0] < x_grid + (1 % N_col + 1) * w and (s[1] >= y_grid + 1 // N_col * h and s[1] < y_grid + (1 // N_col + 1) * h)) else E_x2 if s[0] >= x_grid + 2 % N_col * w and (s[0] < x_grid + (2 % N_col + 1) * w and (s[1] >= y_grid + 2 // N_col * h and s[1] < y_grid + (2 // N_col + 1) * h)) else E_x3 if s[0] >= x_grid + 3 % N_col * w and (s[0] < x_grid + (3 % N_col + 1) * w and (s[1] >= y_grid + 3 // N_col * h and s[1] < y_grid + (3 // N_col + 1) * h)) else E_x4 if s[0] >= x_grid + 4 % N_col * w and (s[0] < x_grid + (4 % N_col + 1) * w and (s[1] >= y_grid + 4 // N_col * h and s[1] < y_grid + (4 // N_col + 1) * h)) else E_x5 if s[0] >= x_grid + 5 % N_col * w and (s[0] < x_grid + (5 % N_col + 1) * w and (s[1] >= y_grid + 5 // N_col * h and s[1] < y_grid + (5 // N_col + 1) * h)) else 0.0) + s[3] * (B_0 if s[0] >= x_grid + 0 % N_col * w and (s[0] < x_grid + (0 % N_col + 1) * w and (s[1] >= y_grid + 0 // N_col * h and s[1] < y_grid + (0 // N_col + 1) * h)) else B_1 if s[0] >= x_grid + 1 % N_col * w and (s[0] < x_grid + (1 % N_col + 1) * w and (s[1] >= y_grid + 1 // N_col * h and s[1] < y_grid + (1 // N_col + 1) * h)) else B_2 if s[0] >= x_grid + 2 % N_col * w and (s[0] < x_grid + (2 % N_col + 1) * w and (s[1] >= y_grid + 2 // N_col * h and s[1] < y_grid + (2 // N_col + 1) * h)) else B_3 if s[0] >= x_grid + 3 % N_col * w and (s[0] < x_grid + (3 % N_col + 1) * w and (s[1] >= y_grid + 3 // N_col * h and s[1] < y_grid + (3 // N_col + 1) * h)) else B_4 if s[0] >= x_grid + 4 % N_col * w and (s[0] < x_grid + (4 % N_col + 1) * w and (s[1] >= y_grid + 4 // N_col * h and s[1] < y_grid + (4 // N_col + 1) * h)) else B_5 if s[0] >= x_grid + 5 % N_col * w and (s[0] < x_grid + (5 % N_col + 1) * w and (s[1] >= y_grid + 5 // N_col * h and s[1] < y_grid + (5 // N_col + 1) * h)) else 0.0)), κ * ((E_y0 if s[0] >= x_grid + 0 % N_col * w and (s[0] < x_grid + (0 % N_col + 1) * w and (s[1] >= y_grid + 0 // N_col * h and s[1] < y_grid + (0 // N_col + 1) * h)) else E_y1 if s[0] >= x_grid + 1 % N_col * w and (s[0] < x_grid + (1 % N_col + 1) * w and (s[1] >= y_grid + 1 // N_col * h and s[1] < y_grid + (1 // N_col + 1) * h)) else E_y2 if s[0] >= x_grid + 2 % N_col * w and (s[0] < x_grid + (2 % N_col + 1) * w and (s[1] >= y_grid + 2 // N_col * h and s[1] < y_grid + (2 // N_col + 1) * h)) else E_y3 if s[0] >= x_grid + 3 % N_col * w and (s[0] < x_grid + (3 % N_col + 1) * w and (s[1] >= y_grid + 3 // N_col * h and s[1] < y_grid + (3 // N_col + 1) * h)) else E_y4 if s[0] >= x_grid + 4 % N_col * w and (s[0] < x_grid + (4 % N_col + 1) * w and (s[1] >= y_grid + 4 // N_col * h and s[1] < y_grid + (4 // N_col + 1) * h)) else E_y5 if s[0] >= x_grid + 5 % N_col * w and (s[0] < x_grid + (5 % N_col + 1) * w and (s[1] >= y_grid + 5 // N_col * h and s[1] < y_grid + (5 // N_col + 1) * h)) else 0.0) - s[2] * (B_0 if s[0] >= x_grid + 0 % N_col * w and (s[0] < x_grid + (0 % N_col + 1) * w and (s[1] >= y_grid + 0 // N_col * h and s[1] < y_grid + (0 // N_col + 1) * h)) else B_1 if s[0] >= x_grid + 1 % N_col * w and (s[0] < x_grid + (1 % N_col + 1) * w and (s[1] >= y_grid + 1 // N_col * h and s[1] < y_grid + (1 // N_col + 1) * h)) else B_2 if s[0] >= x_grid + 2 % N_col * w and (s[0] < x_grid + (2 % N_col + 1) * w and (s[1] >= y_grid + 2 // N_col * h and s[1] < y_grid + (2 // N_col + 1) * h)) else B_3 if s[0] >= x_grid + 3 % N_col * w and (s[0] < x_grid + (3 % N_col + 1) * w and (s[1] >= y_grid + 3 // N_col * h and s[1] < y_grid + (3 // N_col + 1) * h)) else B_4 if s[0] >= x_grid + 4 % N_col * w and (s[0] < x_grid + (4 % N_col + 1) * w and (s[1] >= y_grid + 4 // N_col * h and s[1] < y_grid + (4 // N_col + 1) * h)) else B_5 if s[0] >= x_grid + 5 % N_col * w and (s[0] < x_grid + (5 % N_col + 1) * w and (s[1] >= y_grid + 5 // N_col * h and s[1] < y_grid + (5 // N_col + 1) * h)) else 0.0))]
    
    r = scipy.integrate.ode(f)
    r.set_integrator("dopri5", atol=1.0e-6, rtol=1.0e-6)
    r.set_initial_value([x_0, y_0, v_x0, v_y0], 0.0)
    s = [[x_0, y_0, v_x0, v_y0]]
    while r.successful() and r.t < t_final:
        r.integrate(r.t + t_final / 1000.0)
        s.append(r.y.tolist())
    
    return s

## \brief Calculates time of detector hit (s)
# \param d_orient detector orientation
# \param d_pos detector line position (m)
# \param d_start detector start coordinate (m)
# \param d_len detector line length (m)
# \param t_final final simulation time (s)
# \param s dependent variables (m)
# \return time of detector hit (s)
def func_t_hit(d_orient, d_pos, d_start, d_len, t_final, s):
    return DetectorHit.det_t_hit(s, d_orient, d_pos, d_start, d_len, t_final)

## \brief Calculates x-coordinate of impact point (m)
# \param d_orient detector orientation
# \param d_pos detector line position (m)
# \param d_start detector start coordinate (m)
# \param d_len detector line length (m)
# \param t_final final simulation time (s)
# \param s dependent variables (m)
# \return x-coordinate of impact point (m)
def func_x_hit(d_orient, d_pos, d_start, d_len, t_final, s):
    return DetectorHit.det_x_hit(s, d_orient, d_pos, d_start, d_len, t_final)

## \brief Calculates y-coordinate of impact point (m)
# \param d_orient detector orientation
# \param d_pos detector line position (m)
# \param d_start detector start coordinate (m)
# \param d_len detector line length (m)
# \param t_final final simulation time (s)
# \param s dependent variables (m)
# \return y-coordinate of impact point (m)
def func_y_hit(d_orient, d_pos, d_start, d_len, t_final, s):
    return DetectorHit.det_y_hit(s, d_orient, d_pos, d_start, d_len, t_final)
